---
target: Coast app (Dashboard, VehicleDetail, Check, ShopBrief) — re-run
total_score: 36
p0_count: 0
p1_count: 0
timestamp: 2026-07-19T18-39-15Z
slug: src
---
Method: dual-agent (A: a53ff352c1249142b · B: a14ee81def9f3b3d5)

Target: Coast app (Dashboard, VehicleDetail, Check, ShopBrief) — re-run after the P0/P1/P2/P3 fix pass.

## Fix Verification (independently re-checked, not trusted on claim)

| Item | Verdict |
|---|---|
| P0 Earned Green Rule (odometer-only vehicle reads green) | **FIXED, defense-in-depth.** `verdict.ts:57-61,324-336` gates on real `lastDone`; `reminderEngine.ts:231-269` independently clamps the never-serviced projection so it can never resolve to `completed`. Two layers — a regression in one wouldn't silently reopen the bug. Traced the full data flow for odometer-only/zero-history: cannot reach green anywhere (`Vehicles.tsx`, `VehicleDetail.tsx`, `Dashboard.tsx`, `health.ts` all confirmed). |
| P1 Amber panel contrast (2.77:1) | **FIXED.** `.cv-panel.cv-amber` now uses `--sig-amber-ink (#8a5800)` → measured **6.04:1** live. The bright `--sig-amber` is now decorative-only (ruler segment, dots), never text-bearing. |
| P1 HealthMeter/lamp redundancy | **FIXED.** Component fully removed, zero references anywhere. VehicleDetail leads with exactly one instrument (panel + ruler); per-row pill + progress bar is legitimate depth, not a restatement. |
| P1 Check ordering (drive/tow under cost) | **FIXED.** `ActionCards` renders directly after the verdict, before "Most likely"/cost. Comment cites PRODUCT.md's "err urgent on safety" verbatim. |
| P2/P3 stat-tile grid, olive residue, duplicate tokens, silent share failure, reduced-motion gaps, gradient/side-stripe row | **ALL FIXED**, verified individually — plain `.gb-links` row, no stray green hex anywhere, one `:root` block, `role="alert"` share-failure notice with correct `AbortError` exception, comprehensive reduced-motion coverage, flat-tint overdue row. |

**All 12 previously-failing contrast pairs re-measured and now pass** (4.42–6.29:1 depending on pair; full table below). Zero new console errors on any route.

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 4 | Verdict + ruler + confidence note; excellent |
| 2 | Match System / Real World | 4 | Driver's words in, mechanic language out |
| 3 | User Control and Freedom | 3 | Confirm-gated deletes have no undo after confirming |
| 4 | Consistency and Standards | 3 | VehicleDetail rows use raw `STATUS_LABELS` ("Watch next") while Dashboard/Vehicles use `BAND_LABELS` ("Can coast") for the identical state — new finding, see below |
| 5 | Error Prevention | 4 | `parseNumberInput`, two-step `ConfirmButton`, non-native validation |
| 6 | Recognition Rather Than Recall | 4 | Per-option hints, per-row reason strings, "what to bring" |
| 7 | Flexibility and Efficiency | 3 | No shortcut back into a familiar triage path for a returning user |
| 8 | Aesthetic and Minimalist Design | 4 | Signal Budget honored throughout, no decoration |
| 9 | Error Recovery | 4 | Share failure surfaced, ErrorBoundary, notice-error |
| 10 | Help and Documentation | 3 | Strong first-run; no in-context help identifying a warning-light symbol |
| **Total** | | **36/40** | **Excellent — minor polish only** |

(Note: heuristic 4 was scored 3 rather than higher specifically because of the newly-found vocabulary split below — everything else in "consistency" is strong.)

## Named-Principle Adjudication

**The Earned Green Rule — PASS on every surface**, confirmed independently at all 6 call sites (`verdict.ts`, `reminderEngine.ts`, `Vehicles.tsx`, `Dashboard.tsx`, `VehicleDetail.tsx`, `health.ts`). No data state (odometer-only, empty, mixed) produces a green/all-clear read for a never-serviced vehicle. This is now the strongest-enforced principle in the codebase — encoded at the engine layer, not just the UI.

**One verdict first, depth on demand — PASS, with one consistency crack.** Every screen leads with a single `cv-panel` verdict; no instrument restates another. The crack: VehicleDetail's row pills speak the engine's raw status vocabulary while every other surface speaks the band vocabulary — same state, two different words depending on which screen you're on. This is a Consistency/Earned-Familiarity issue, not a "two instruments" issue, but it's real.

## Anti-Patterns Verdict

**LLM assessment:** Not slop, decisively. The three-voice typography discipline is enforced in CSS, not just described — verdicts in Overpass, explanations in Newsreader serif, every number in Overpass Mono tabular figures, with zero leaks either direction. Copy stays situational and humane under pressure (a 19-week-old fix-now concern reframes from a stale date to "19 weeks ago" rather than looking abandoned). The anti-references (stat-grid, upsell, red-alert overload) are actively and consistently avoided now.

**Deterministic scan:** `detect.mjs --json src` → exit 0, **151 findings, 0 errors** (103 font-size + 41 radius + 5 color advisories, 2 `layout-transition` warnings — both on documented meter-fill exceptions). Down from 160 findings/12 color hits in the prior run — the color-token cleanup measurably reduced the advisory count.

## Overall Impression

This closed the gap that mattered most: the Earned Green Rule is no longer just well-narrated, it's structurally unbreakable — enforced at the reminder-engine layer independently of the verdict layer, so a future refactor of one can't silently reopen the other. Contrast is clean across every previously-failing pair. The remaining issues are real but genuinely minor: one missed contrast pair in a component the fix pass didn't touch, one zoom-overflow bug in a different component with the identical root cause as the fixed one, and a vocabulary inconsistency between two label sets that both already exist in the codebase.

**Biggest opportunity:** the `.account-bar` zoom-overflow fix and the amber-ink contrast fix were both narrowly scoped to the specific components flagged in the original critique. Both bugs turned out to be a *pattern* (flex-item content-width floor; text-bearing color needing the ink variant) that recurs elsewhere in the file. A sweep for the same two patterns across the rest of `app.css` would likely catch a few more instances before they get independently rediscovered one at a time.

## What's Working

1. **The Earned Green Rule is now defense-in-depth.** The engine clamp (`reminderEngine.ts:243-249`) and the verdict gate (`verdict.ts:324-336`) are two independent layers enforcing the same rule — a regression in one doesn't silently break the guarantee. This is the clearest example in the codebase of a design principle being encoded in architecture rather than left to UI discipline.
2. **Typographic voice enforcement survived contact with the real component tree.** Cost figures stay mono/tabular even in the densest spots (the quote-check rating, the cost table), sentences stay serif, tags stay mono-uppercase — no exceptions found on re-sweep.
3. **Drive-vs-tow is now correctly the first thing after the verdict**, with `role="alert"` on the tow case and a code comment citing the PRODUCT.md principle by name — the fix is legible as intentional, not incidental.

## Priority Issues (new — not among the six previously-fixed items)

**[P2] New contrast failure: `.qc-yn.is-on`, 4.42:1** (`app.css:3762-3766`) — the Check flow's yes/no toggle button (sig-green text on a 14% tint), just under the 4.5:1 bar the rest of the palette now clears. Likely the same "text color needs the -ink variant" pattern as the amber fix, just unswept.
**Fix:** darken this specific text color slightly (same approach as `--sig-amber-ink`), or route it through an existing darker green token if one fits.
→ `/impeccable audit`

**[P2] New zoom-overflow bug: `.quick-add-bar`, 21px overflow at 375px + 200% zoom** (`app.css:534-538`) — same root cause family as the just-fixed `.account-bar` bug: `flex-wrap: nowrap` + `flex: 1` buttons with no `min-width: 0`, so the row's content floor exceeds the viewport once text scales. Only reproduces at the mobile-width + 200%-zoom combination, which the original fix's test matrix (desktop zoom, mobile 100%) didn't cover together.
**Fix:** apply the identical fix pattern used on `.account-bar` — `min-width: 0` on the flex items, or let the row wrap.
→ `/impeccable audit`

**[P2] Split status vocabulary: VehicleDetail rows vs. every other surface** (`VehicleDetail.tsx:212` uses `STATUS_LABELS` → "Watch next"/"Due next"; `Vehicles.tsx:58`/Dashboard use `BAND_LABELS` → "Can coast"/"Book soon" for the identical state). DESIGN.md §6 says the band vocabulary "means the same thing everywhere" — this is the one place it's remapped.
**Fix:** route VehicleDetail's row pill through `bandFromStatus` → `BAND_LABELS`, matching every other surface.
→ `/impeccable clarify`

**[P2] TodayCard is one giant anchor wrapping two same-level headings** (`Dashboard.tsx:135-171`) — the whole card is a single `<a>` containing both the vehicle-name `<h3>` and the verdict headline `<h3>`. A screen reader announces one long link with two same-level headings and the full coast list as link text.
**Fix:** demote one heading level, and consider whether the whole card needs to be one giant tap target versus a named "Open Service Record" link.
→ `/impeccable audit`

**[P3] Triage option count still exceeds 4 at peak stress** (6 top-level playbooks, 7 warning-light options) — unchanged from the prior pass, not part of the claimed fix set, still mitigated by per-option plain-English hints.
→ `/impeccable layout` (optional, low urgency)

## Persona Red Flags

**Jordan (first-timer):** Mostly safe now — first-run is an explicit 3-step path, and never-serviced vehicles honestly say "Not set up yet" with a setup link. Remaining flag: the 6-way triage picker still has no "I'm not sure" escape.

**Casey (one-handed, parking lot):** Mostly safe — verdict-first, sticky log button, single-vehicle auto-skip on Check. New flag: the just-found `.quick-add-bar` zoom-overflow means a low-vision Casey using 200% text zoom on a phone gets 21px of horizontal scroll on VehicleDetail specifically (the account-bar fix didn't cover this).

**Sam (screen reader, 200% zoom):** Largely strong — `role="img"`/aria-label on the ruler, `role="alert"`/`role="status"` correctly placed, labeled inputs throughout, full reduced-motion coverage now confirmed comprehensive. New flags: the TodayCard double-heading-in-one-link structure (P2 above), and the `.qc-yn.is-on` contrast miss would affect Sam's low-vision reading of the quote-check toggle specifically.

## Minor Observations

- The garage lamp (`Dashboard.tsx:79-91`, `.vd-lamp is-overdue/is-na/is-ok`) is a small dot-lamp vocabulary that exists only on that one line and nowhere else in the four-signal system — worth folding into the shared vocabulary or leaving as a deliberately distinct "garage-level" glance, but currently undocumented either way.
- `--c-watch` doing double duty as both the watch/coast pill ink and `--sig-blue-deep` is elegant reuse from the token-merge fix, but means a future tweak to one silently moves the other — worth a one-line comment at the declaration site.
- `health.ts`'s 0–100 `score` has no visible consumer since `HealthMeter` was removed — worth confirming it's genuinely dead before it tempts someone to resurrect the meter that was just deleted for being redundant.

## Questions to Consider

1. **If the Earned Green Rule is this sacred, does a never-set-up vehicle's full schedule of blue "Watch next" rows also narrate knowledge the app doesn't have** — a softer version of the same problem the not-set-up verdict just refused to commit? Worth deciding explicitly whether "watch next, projected" reads as honest enough on its own, or needs the same "not real data" framing the verdict panel gets.
2. **Two bugs (contrast-needs-ink, zoom-overflow-needs-min-width) each recurred once in a component the original fix didn't touch.** Is a targeted "sweep the whole file for this exact pattern" pass worth doing now, before the next one gets independently rediscovered?
