# Active Handoff

Shared state between Claude Code and Codex on this repo. Read this before
starting work; update it when you finish. Keep this file to the *current*
task only — archive finished entries by deleting them once merged into
CLAUDE.md's "Next up"/architecture sections, git history is the record.

## Current task

Implement the onboarding polish work:
- Design spec: `docs/superpowers/specs/2026-07-22-onboarding-polish-design.md`
  (AddVehicle restyle to match the app's editorial visual language + a
  first-vehicle welcome banner on VehicleDetail).
- Implementation plan: `docs/superpowers/plans/2026-07-22-onboarding-polish.md`
  (the plan now exists — check whether it's been executed against before
  starting, this file may be stale relative to it).

## Context

- Spec + plan are written; implementation status against the plan is
  unconfirmed — verify against the actual code before assuming nothing's done.
- Don't invent new feature milestones beyond what the spec/plan cover.

## Done

(unconfirmed — check the plan file's own status markers and the code)

## Next

- Read the plan, confirm scope/status with David if anything's ambiguous,
  then implement or continue.
